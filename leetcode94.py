class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


def iteratively(root):
    result = []
    temp=[]
    temp.append(root)
    if temp
a = TreeNode(1)
a.left = None
a.right = TreeNode(2)
a.right.left = TreeNode(3)
